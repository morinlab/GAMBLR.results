% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_cn_segments.R
\name{annotate_focal_and_arm_level_CNV}
\alias{annotate_focal_and_arm_level_CNV}
\title{Annotate focal and arm-level CNVs}
\usage{
annotate_focal_and_arm_level_CNV(
  seg_data,
  these_samples_metadata,
  focal_peak_file = "DLBCL_focal_peaks.18Aug2024.tsv",
  broad_peak_file = "DLBCL_broad_significance.18Aug2024.tsv",
  single_del_threshold = 1.3,
  double_del_threshold = 0.9,
  single_amp_threshold = 2.5,
  double_amp_threshold = 3.5,
  adjust_for_ploidy = FALSE,
  rounded = TRUE
)
}
\arguments{
\item{seg_data}{Copy-number segments, typically from \code{\link[=get_cn_segments]{get_cn_segments()}}, with columns
\code{ID}, \code{chrom}, \code{start}, \code{end}, and \code{CN} (and optionally \code{log.ratio}/\code{adjusted_CN}).}

\item{these_samples_metadata}{Sample metadata used by \link[GAMBLR.utils:segmented_data_to_cn_matrix]{GAMBLR.utils::segmented_data_to_cn_matrix}
to align sample IDs and seq_type.}

\item{focal_peak_file}{Path to the focal GISTIC peaks file (default
\code{DLBCL_focal_peaks.18Aug2024.tsv}; hg38 automatically switches to the lifted file).}

\item{broad_peak_file}{Path to the arm-level GISTIC significance file (default
\code{DLBCL_broad_significance.18Aug2024.tsv}; hg38 automatically switches to the lifted file).}

\item{single_del_threshold}{Absolute CN threshold used for a single-copy loss call.}

\item{double_del_threshold}{Absolute CN threshold used for a homozygous loss call.}

\item{single_amp_threshold}{Absolute CN threshold used for a single-copy gain call.}

\item{double_amp_threshold}{Absolute CN threshold used for a high-level gain call.}

\item{adjust_for_ploidy}{Pass through to \link[GAMBLR.utils:segmented_data_to_cn_matrix]{GAMBLR.utils::segmented_data_to_cn_matrix}; set TRUE
to center CN values on each sample’s average ploidy.}

\item{rounded}{Currently unused (kept for backward compatibility).}
}
\value{
A list with matrices/vectors:
\itemize{
\item \code{gsm}: thresholded focal + arm scores after adjusting focal calls for arm-level background.
\item \code{not_arm_adjusted}: thresholded scores without focal adjustment.
\item \code{all_ones}: same as \code{gsm} but high-level events collapsed to 1 (present/absent).
\item \code{cnv}: arm-level CN matrix (unthresholded) for significant arms.
\item \code{focal_cnv}: focal CN matrix (unthresholded) for significant focal peaks.
\item \code{unthresholded_gsm}: raw CN values for all arm and focal regions.
\item \code{arm_level_deletions}, \code{arm_level_gains}, \code{arm_level_gains_old}: arm-level scores (0/1/2) with
and without length filtering.
\item \code{focal_deletions}, \code{focal_gains}: focal scores (0/1/2) before adjustment for arm background.
}
}
\description{
Convert segmented CN data into arm- and focal-level gain/loss calls using
default DLBCL significance peaks (or user-supplied peak files). Produces both raw
copy-number matrices and thresholded “GSM” (genome state matrix) scores.
}
\details{
Arm events must span ≥50\% of the chromosome arm to count. Focal deletions are
adjusted upward by arm losses; focal gains are adjusted downward by arm gains to avoid
double-counting. Uses hg38 peak files automatically when \code{seg_data} reports hg38.
}
\examples{
\dontrun{
segs <- get_cn_segments(these_samples_metadata = metas, projection = "grch37")
gsm_out <- annotate_focal_and_arm_level_CNV(segs, metas)
head(gsm_out$gsm)
}
}
