% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_cn_segments.R
\name{assign_segment_to_arm}
\alias{assign_segment_to_arm}
\title{Annotate CN segments with chromosome arms}
\usage{
assign_segment_to_arm(seg_data, genome_build)
}
\arguments{
\item{seg_data}{Data frame of CN segments with \code{chrom}, \code{start}, and \code{end}
columns; build is inferred via \code{get_genome_build(seg_data)}.}

\item{genome_build}{Optional build hint (currently unused; kept for compatibility).}
}
\value{
\code{seg_data} with arm annotations, including \code{chromosome}, \code{band_start},
\code{band_end}, and \code{arm_name} (e.g., \verb{1p}, \verb{8q}). Overlapping arms (rare) will
yield multiple rows per segment.
}
\description{
Map each copy-number segment to its chromosome arm (p/q) for the
appropriate genome build (grch37 or hg38).
}
\details{
Chooses \code{chromosome_arms_grch37} or \code{chromosome_arms_hg38}, then uses
\code{cool_overlaps} to assign segments to arms; \code{arm_name} is the chromosome plus
uppercased arm label.
}
\examples{
\dontrun{
segs <- get_cn_segments(these_samples_metadata = metas)
segs_arm <- assign_segment_to_arm(segs)
table(segs_arm$arm_name)
}
}
