% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collate_results_db.R
\name{collate_results_db}
\alias{collate_results_db}
\title{Collate GAMBL results via the SQLite-backed cache.}
\usage{
collate_results_db(these_samples_metadata, refresh = list(), db_path = NULL)
}
\arguments{
\item{these_samples_metadata}{A metadata table with (at least)
\code{sample_id} and \code{seq_type} columns. Defaults to all genome and capture
samples from \link{get_gambl_metadata} if omitted.}

\item{refresh}{Optional named list, keyed by registry name (e.g.
\code{"ssm_results"}), of sample_ids to force-recompute for that function
even if already present in its table. Functions not named here use the
default missing-only behaviour.}

\item{db_path}{Optional explicit path to the SQLite database, passed to
\code{gambl_collated_db()}.}
}
\value{
\code{these_samples_metadata} joined with every registered
function's result columns.
}
\description{
The SQLite-backed counterpart to
\link{collate_results}. For each function registered in the
internal \code{collate_registry}, computes results only for samples missing
from that function's table (or explicitly listed in \code{refresh}), then
returns one wide table joining every registered function's results onto
\code{these_samples_metadata}.
}
\details{
This is additive, not a replacement: \code{collate_results()} and
the individual \verb{collate_*_results()} wrapper functions are untouched and
keep working exactly as before, backed by the existing shared-TSV cache.
Only functions listed in the internal \code{collate_registry} are included
here -- see \code{R/collate_registry.R} for which ones and why.
}
\examples{
\dontrun{
my_meta <- get_gambl_metadata() \%>\% dplyr::filter(pathology == "FL")
collated <- collate_results_db(these_samples_metadata = my_meta)

# force ssm_results to recompute for two specific samples
collated <- collate_results_db(
  these_samples_metadata = my_meta,
  refresh = list(ssm_results = c("sample1", "sample2"))
)
}
}
